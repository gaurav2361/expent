[package]
name    = "ocr"
version = "0.1.0"
edition = "2024"

[dependencies]
anyhow    = "1.0"
reqwest   = { features = [ "json", "multipart" ], version = "0.12" }
serde     = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
tracing   = "0.1"
