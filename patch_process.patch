--- crates/expent_core/src/services/ocr/process.rs
+++ crates/expent_core/src/services/ocr/process.rs
@@ -14,24 +14,11 @@ pub async fn process_ocr(
     let user_id = user_id.to_string();
     db.transaction::<_, OcrTransactionResponse, AppError>(|txn_db| {
         Box::pin(async move {
-<<<<<<< HEAD
-            let mut contact_id = processed
-                .data
-                .0
-                .get("contact_id")
-                .and_then(|v| v.as_str())
-                .map(std::string::ToString::to_string);
-
-            let wallet_id = processed
-                .data
-                .0
-                .get("wallet_id")
-                .and_then(|v| v.as_str())
-                .map(std::string::ToString::to_string);
-=======
->>>>>>> origin/main
             let mut contact_created = false;

             if processed.doc_type == "GPAY" {
